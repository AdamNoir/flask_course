
    <h3>Precio Regular</h3>
    {% for product in products if product['product_price'] and product['product_price'] > 1000 %}
        <ul>
            <li>
                <a href="{{ url_for('products.product_by_id', product_id=product['product_id']) }}">{{ product['product_name'] }}</a>
            </li>
        </ul>
    {% endfor %}

    <h3>Precio en oferta</h3>
    {% for product in products if product['product_price'] and product['product_price'] < 1000 %}
        <ul>
            <li>
                <a href="{{ url_for('products.product_by_id', product_id=product['product_id']) }}">{{ product['product_name'] }}</a>
            </li>
        </ul>
    {% endfor %}

    <h3>Precio no disponible</h3>
    {% for product in products if product['product_price'] is none %}
        <ul>
            <li>
                <a href="{{ url_for('products.product_by_id', product_id=product['product_id']) }}">{{ product['product_name'] }}</a>
            </li>
        </ul>
    {% endfor %}
